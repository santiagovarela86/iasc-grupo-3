version: "3.7"

services:
  router:
    build: ./Router
    stdin_open: true
    tty: true
    networks:
      iasc-network:
        aliases:
          - router

  server:
    build: ./Server
    stdin_open: true
    tty: true
    networks:
      iasc-network:
        aliases:
          - server
    depends_on:
      - router

networks:
  iasc-network:
    driver: bridge
